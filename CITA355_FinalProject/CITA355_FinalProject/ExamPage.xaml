<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CITA355_FinalProject.ExamPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            
            
            <!--Multiple Choice Question-->
            <Border
                StrokeThickness="1"
                Padding="15">

                <VerticalStackLayout Spacing="12">

                    <!--Multiple Choice-->
                    <Label
                        Text="1. What does PHP stand for?"
                        FontSize="18"
                        FontAttributes="Bold"
                        LineBreakMode="WordWrap"/>

                    <!--Answers-->
                    <VerticalStackLayout x:Name="Question1" RadioButtonGroup.GroupName="Question1"
                        Padding="20,0,0,0"
                        Spacing="10">

                        <RadioButton Content="Personal Home Page" Value="a" />
                        <RadioButton Content="Private Home Page" Value="b" />
                        <RadioButton Content="Preprocessor Hypertext" Value="c" />
                        
                    </VerticalStackLayout>

                </VerticalStackLayout>

            </Border>

            <!--Checkbox Question-->
            <Border
                StrokeThickness="1"
                Padding="15">

                <VerticalStackLayout Spacing="12">

                    <!-- Question -->
                    <Label
                        Text="2. Which are server-side languages?"
                        FontSize="18"
                        FontAttributes="Bold"
                        LineBreakMode="WordWrap"/>

                    <!--Answers-->
                    <VerticalStackLayout
                        Padding="20,0,0,0"
                        Spacing="10">

                        <HorizontalStackLayout Spacing="10">
                            <CheckBox x:Name="checkboxPHP"/>
                            <Label Text="PHP"
                                   VerticalOptions="Center"/>
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Spacing="10">
                            <CheckBox x:Name="checkboxJavascript"/>
                            <Label Text="Javscript"
                                   VerticalOptions="Center"/>
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Spacing="10">
                            <CheckBox x:Name="checkboxPython"/>
                            <Label Text="Python"
                                   VerticalOptions="Center"/>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>

                </VerticalStackLayout>
            </Border>

            <!--Multiple Choice Question-->
            <Border
                StrokeThickness="1"
                Padding="15">

                <VerticalStackLayout 
                    Spacing="12">

                    <!--Multiple Choice-->
                    <Label
                        Text="3. Which database is commonly used with PHP?"
                        FontSize="18"
                        FontAttributes="Bold"
                        LineBreakMode="WordWrap"/>

                    <!--Answers-->
                    <VerticalStackLayout x:Name="Question3" RadioButtonGroup.GroupName="Question3"
                        Padding="20,0,0,0"
                        Spacing="10">

                        <RadioButton Content="MySQL" Value="mysql"/>
                        <RadioButton Content="Oracle" Value="oracle"/>
                    </VerticalStackLayout>

                </VerticalStackLayout>

            </Border>

            <!--Dropdown Question-->
            <Border
                StrokeThickness="1"
                Padding="15">

                <VerticalStackLayout Spacing="12">

                    <!--Question-->
                    <Label
                        Text="4. What tag is used in HTML forms?"
                        FontSize="18"
                        FontAttributes="Bold"
                        LineBreakMode="WordWrap"/>

                    <!--Dropdown Selection-->
                    <Picker x:Name="pickerQuestion4"
                        Title="Select an answer"
                        Margin="20,0,0,0">

                        <Picker.Items>
                            <x:String>form</x:String>
                            <x:String>input</x:String>
                            <x:String>body</x:String>
                        </Picker.Items>

                    </Picker>

                </VerticalStackLayout>
            </Border>

            <!--Multiple Choice Question-->
            <Border
                StrokeThickness="1"
                Padding="15">

                <VerticalStackLayout Spacing="12">

                    <!--Multiple Choice-->
                    <Label
                        Text="5. What does SQL stand for?"
                        FontSize="18"
                        FontAttributes="Bold"
                        LineBreakMode="WordWrap"/>

                    <!--Answers-->
                    <VerticalStackLayout x:Name="Question5" RadioButtonGroup.GroupName="Question5"
                        Padding="20,0,0,0"
                        Spacing="10">

                        <RadioButton Content="Structured Query Language" Value="a"/>
                        <RadioButton Content="Simple Query Language" Value="b"/>
                    </VerticalStackLayout>

                </VerticalStackLayout>

            </Border>

            <Button Text="Submit" Clicked="OnSubmitExam"/>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
